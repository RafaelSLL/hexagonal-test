spring:
  application:
    name: hexagonal

  datasource:
    url: jdbc:postgresql://localhost:5432/hexagonal
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
